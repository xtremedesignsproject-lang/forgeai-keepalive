# Public market data works without variables.
# These are required only when the Trade Card and journal screens are connected to Supabase.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Never expose the service role to the browser.
SUPABASE_SERVICE_ROLE_KEY=
