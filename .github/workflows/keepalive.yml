name: ForgeAI Keepalive Ping

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keepalive endpoint
        run: |
          curl -fsS "https://forgeai.manus.space/api/trigger-scan/status" >/dev/null
